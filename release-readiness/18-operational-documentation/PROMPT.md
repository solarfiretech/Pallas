# Prompt: Reconcile and complete operational documentation

Implement release task 18 for Pallas. Reconcile README, design, developer guide, release contract, environment reference, and actual tested behavior. Document the OpenPLC work-directory volume, all environment variables, separate development and release commands, health semantics, startup timing, troubleshooting, backup/restore, upgrade/rollback, and destructive reset warnings.

Make core documentation self-contained and state that example applications are optional. Remove stale or contradictory claims. Verify every command and link against the current repository and release configuration. Do not change runtime behavior except to correct a documentation-discovered defect that prevents accurate docs; report such a defect before expanding scope.
