# Prompt: Define the release contract

Inspect the current Pallas repository and define the release contract for version 0.1.0. Document supported Docker Engine and Compose versions, supported host platforms, required services, ports, minimum host resources, startup and shutdown expectations, persistence guarantees, and the boundary between development and release-supported behavior. Treat the `Examples` directory as optional and exclude examples from release completion requirements.

Keep this task documentation-only. Reconcile the contract with the existing README, design, developer guide, and Compose topology without implementing later release-readiness work. State unresolved decisions explicitly. Validate all commands and file references you add, then report changed files, validation performed, assumptions, and remaining risks.
